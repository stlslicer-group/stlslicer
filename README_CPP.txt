Main function is in STLslicer.cpp, where a basic code flow is presented. 
The model interface returns bool values to let the caller know if the operation was successful. 
Include "Model.h" wherever you wish to use. 

